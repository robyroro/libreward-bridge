# Third-party notices

Runtime dependencies are Node.js packages under permissive licenses (MIT, ISC, BSD, or Apache-2.0) as recorded by the lockfile and CI license check. GNU Taler wallet-core and `taler-wallet-cli` are optional external runtime components and are not copied into this tree. GNU Taler components are GPLv3+; their use motivated the compatible AGPL-3.0-or-later server license. Documentation references remain subject to GNU Taler's stated GPLv3+/GFDL terms. Generate the release SBOM with `npm run sbom` and attach it to every release.
